strategy:
  type: proxy
  commandToRun: npm run test
  targetHost: localhost
  targetPort: 3005

api:
  id: example-app
  security:
    - type: bearer
  paths:
    - /users
    - /users/login
    - /users/:userId/followers
